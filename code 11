#include <stdio.h>

int main() {
    int n,i,sum=0,a[100];
    printf("enter range");
    scanf("%d",&n);
    sum=(n*(n+1))/2;
    for(i=0;i<n-1;i++)
    { scanf("%d",&a[i]);
    }
    for(i=0;i<n-1;i++)
    { sum=sum-a[i];
    }
    printf("%d",sum);

    return 0;
}
